# --- Challenge dataset (large; provided separately — never commit) ---
# The full bundle lives in data/ (candidates.jsonl ~465 MB, validator, schema, samples, JD).
data/
*.jsonl
*.jsonl.gz
*.zip
# ...but the small curated demo sample for the sandbox IS committed:
!app/demo_data/demo_candidates.jsonl

# --- Separate prior project (not part of this repo) ---
old/

# --- Precomputed / intermediate artifacts (regenerate via scripts) ---
artifacts/
embeddings/
*.npy
*.faiss
*.pkl
*.index

# --- Generated submissions (keep the code, not the output) ---
submission*.csv

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# --- OS / editor ---
.DS_Store
__MACOSX/
.vscode/
.idea/
