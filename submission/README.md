# Submission assets

- `Idea Submission Template _ Redrob.pptx` — the organizer-provided idea-submission deck template
  (copied here for convenience; **git-ignored** to keep the repo free of large binaries). Fill it and
  upload the deck to the Hack2Skill portal, not to this repo.

The scored deliverables live elsewhere in the repo:
- ranked output: produced by `just rank` → `submission.csv` (git-ignored; regenerate from code)
- methodology/results: `README.md`, `docs/` (esp. `docs/09_results_and_observations.md`)
- portal metadata: `submission_metadata.yaml`
