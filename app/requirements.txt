# Demo backend (sandbox) dependencies — NOT needed for the offline ranking step (rank.py is stdlib-only).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
anthropic>=0.40          # optional: only used by /api/explain when ANTHROPIC_API_KEY is set
